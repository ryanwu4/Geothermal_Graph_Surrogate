name: geothermal-pomdp
channels:
  - defaults
  - pytorch
  - pyg
dependencies:
  - python=3.11
  - numpy>=2.0
  - h5py>=3.10
  - tqdm
  - pip
  - pip:
      - torch>=2.2
      - torch-geometric>=2.5
      - lightning>=2.2
      - pytorch-lightning>=2.2
      - scikit-learn>=1.4
      - scipy>=1.12
      - matplotlib>=3.8
      - seaborn
      - pandas
      - plotly
      - pyyaml
